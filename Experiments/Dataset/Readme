Instances that are out of memory with Mibs are listed in folder DNEGMibs/ BVCMibs/ and BVC_ASYMM_Mibs

Command:

mibs -Alps_instance "$data_folder${file_name}.mps" -MibS_auxiliaryInfoFile "$data_folder${file_name}.txt" -Alps_timeLimit 600 



All dataset used in our code are BVC.tar.gz and DNeg.tar.gz

Please run the following command to extract the datasets:

tar -zxvf BVC.tar.gz

tar -zxvf DNeg.tar.gz

